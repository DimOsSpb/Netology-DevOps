---
%{ for inst in instances ~}
%{ if inst.service_name != "" && inst.external_ip != "" ~}
${inst.service_name}:
  hosts:
    ${inst.instance_name}:
      ansible_host: ${inst.external_ip}
      ansible_user: ${inst.user}
      ansible_ssh_private_key_file: ${inst.pkeyfile}
%{ endif ~}
%{ endfor ~}